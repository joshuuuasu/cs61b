/** An Integer tester created by Flik Enterprises. */
public class Flik {
    //should use (int a, int b) or a.equals(b)
    public static boolean isSameNumber(Integer a, Integer b) {
        return a == b;
    }
}
